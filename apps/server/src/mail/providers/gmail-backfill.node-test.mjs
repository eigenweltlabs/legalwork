import {test} from 'node:test';
import assert from 'node:assert/strict';
import {randomBytes} from 'node:crypto';
import {mkdtemp,rm} from 'node:fs/promises';
import {join} from 'node:path';
import {tmpdir} from 'node:os';
import {GmailBackfill} from './gmail-backfill.js';
import {GmailTransportError} from './gmail.js';
import {MailCredentialRepository} from '../storage/credentials.js';
import {MailRepository} from '../storage/repository.js';
import {MailContentStore} from '../storage/content-store.js';
import {MimeProjectionStore} from '../storage/mime-projection-store.js';
import {openEncryptedMailDatabase} from '../storage/database.js';
import {migrateMailSchema} from '../storage/schema.js';
const delay=ms=>new Promise(r=>setTimeout(r,ms));
async function until(fn){for(let i=0;i<500;i++){const v=fn();if(v)return v;await delay(10);}throw Error('fixture wait exceeded');}
const defer=()=>{let resolve;return{promise:new Promise(r=>resolve=r),resolve:v=>resolve(v)}};
const binding={provider:'gmail',clientId:'synthetic.apps.googleusercontent.com',authority:'https://accounts.google.com',providerSubject:'subject'};
async function fixture(body){
 const dir=await mkdtemp(join(tmpdir(),'gmail-backfill-')),path=join(dir,'mail.sqlite'),key=randomBytes(32);let db=await openEncryptedMailDatabase({path,key});migrateMailSchema(db);const engines=[];
 const creds=()=>new MailCredentialRepository(db,'owner');new MailRepository(db,'owner').createAccount({id:'a',provider:'gmail',displayName:'Fixture'});
 creds().connect('a',binding,null,{accessToken:'synthetic-secret',expiresAt:Date.now()+3600000,grantedScopes:['https://www.googleapis.com/auth/gmail.modify'],refreshToken:{action:'clear'}});
 const calls=[];let rawActive=0,maxRaw=0;
 const transport={async getProfile(){return{historyId:'1'};},async listHistory(input){return{historyId:input.startHistoryId,nextPageToken:null,records:[]};},async getMetadata(id){return{id,threadId:'actual-thread',labelIds:['INBOX'],historyId:db.get('SELECT history_id FROM mail_gmail_presence WHERE account_id=? AND message_key=?',['a',JSON.stringify(['gmail',id])])?.history_id??'1',internalDate:'1000'};},async listLabels(){return{labels:[{id:'INBOX',name:'Inbox',type:'system'}]};},async listMessages(input){calls.push(input);return{messages:input.recentAfterSeconds?[{id:'recent',threadId:'thread'}]:input.pageToken?[{id:'old',threadId:'thread'}]:[{id:'recent',threadId:'thread'},{id:'middle',threadId:'thread'}],nextPageToken:input.recentAfterSeconds||input.pageToken?null:'next'};},async consumeRaw(id,sink){rawActive++;maxRaw=Math.max(maxRaw,rawActive);try{await sink(Buffer.from(`Subject: ${id}\r\n\r\nbody`));return{id,threadId:'actual-thread',labelIds:['INBOX'],historyId:'1',internalDate:'1000',sizeEstimate:20,rawBytes:20};}finally{rawActive--;}}};
 const project=async({accountId,locator,reference,source,work,assertCurrent})=>{for await(const _ of source){assertCurrent();}await new MailContentStore(db,'owner').writePart(accountId,locator,{kind:'body',maxBytes:2},[Buffer.from('{}')],body=>{assertCurrent();work.complete(()=>{assertCurrent();new MimeProjectionStore(db,'owner').complete(accountId,locator,reference.id,{metadata:{subject:'Parsed',from:null,to:null,cc:null,bcc:null,replyTo:null,date:null,messageId:null},bodies:[],attachments:[],body});});});};
 const f={get db(){return db;},creds,calls,transport,project,get maxRaw(){return maxRaw;},make(options={}){const engine=new GmailBackfill({database:db,ownerId:'owner',access:{async acquire(){return creds().readAccess('a',binding);}},transport:()=>transport,projectRaw:project,...options});engines.push(engine);return engine;},async reopen(){for(const e of engines)await e.close();db.close();db=await openEncryptedMailDatabase({path,key});}};
 try{await body(f);}finally{for(const e of engines)await e.close();db.close();key.fill(0);await rm(dir,{recursive:true,force:true});}
}
test('recent then paginated all history deduplicates originals and completes encrypted projections',()=>fixture(async f=>{
 const e=f.make();assert.equal(e.start('a').state,'syncing');assert.equal(e.start('a').state,'syncing');const status=await until(()=>e.status('a').state==='complete'&&e.status('a'));
 assert.equal(status.enumerated,3);assert.equal(status.downloaded,3);assert.equal(status.projected,3);assert.equal(status.failed,0);assert.equal(f.maxRaw,1);assert.ok(f.calls[0].recentAfterSeconds);assert.equal(f.calls[1].recentAfterSeconds,undefined);assert.equal(f.calls[2].pageToken,'next');
 assert.equal(f.db.get("SELECT count(*) AS n FROM mail_memberships").n,3);await f.reopen();assert.equal(f.make().status('a').state,'paused');
}));
test('pause fences late discovery; reopen explicitly resumes same generation',()=>fixture(async f=>{
 const gate=defer(),entered=defer();const e=f.make({transport:()=>({...f.transport,async listMessages(i){entered.resolve();await gate.promise;return f.transport.listMessages(i);}})});e.start('a');await entered.promise;const gen=f.db.get('SELECT generation FROM mail_gmail_runs').generation;e.pause('a');gate.resolve();await delay(30);assert.equal(e.status('a').enumerated,0);await f.reopen();const next=f.make();assert.equal(next.status('a').state,'paused');next.start('a');await until(()=>next.status('a').state==='complete');assert.equal(f.db.get('SELECT generation FROM mail_gmail_runs').generation,gen);
}));
test('missing projector preserves raw and labels but never claims complete',()=>fixture(async f=>{const e=f.make({projectRaw:undefined});e.start('a');const s=await until(()=>e.status('a').state==='attention'&&e.status('a'));assert.equal(s.downloaded,3);assert.equal(s.projected,0);assert.equal(s.error,'content_incomplete');assert.equal(f.db.get('SELECT count(*) AS n FROM mail_memberships').n,3);}));
test('provider retry floor survives reopen without early requests',()=>fixture(async f=>{let calls=0;const e=f.make({transport:()=>({...f.transport,async listLabels(){calls++;throw new GmailTransportError('rate_limited',60000);}})});e.start('a');await until(()=>e.status('a').state==='waiting');const retry=e.status('a').nextRetryAt;await f.reopen();const next=f.make({transport:()=>({...f.transport,async listLabels(){calls++;return f.transport.listLabels();}})});assert.equal(next.status('a').state,'paused');next.start('a');await delay(30);assert.equal(calls,1);assert.equal(next.status('a').nextRetryAt,retry);}));
test('discovery retry budget is persisted and terminal at five failures',()=>fixture(async f=>{const e=f.make({transport:()=>({...f.transport,async listLabels(){throw new GmailTransportError('rate_limited',0);}})});e.start('a');await until(()=>e.status('a').state==='waiting');e.pause('a');await delay(20);f.db.run('UPDATE mail_gmail_runs SET failure_count=4,next_retry_at=NULL');e.start('a');await until(()=>e.status('a').state==='attention');assert.equal(f.db.get('SELECT failure_count FROM mail_gmail_runs').failure_count,5);}));
test('close is bounded and late raw chunks cannot publish after disconnect',()=>fixture(async f=>{const gate=defer(),entered=defer();const e=f.make({transport:()=>({...f.transport,async consumeRaw(id,sink){entered.resolve();await gate.promise;return f.transport.consumeRaw(id,sink);}})});e.start('a');await entered.promise;f.creds().disconnect('a',f.creds().status('a').version);await e.close();gate.resolve();await delay(30);assert.equal(f.db.get('SELECT count(*) AS n FROM mail_blob_publications').n,0);assert.throws(()=>e.start('a'),{code:'closed'});}));
test('scope and configuration failures are fixed; metadata-only accounts cannot start',()=>fixture(async f=>{const e=f.make();assert.throws(()=>e.status('foreign'),{code:'not_found'});assert.throws(()=>f.make({pageSize:0}),{code:'configuration_invalid'});f.creds().disconnect('a',f.creds().status('a').version);assert.throws(()=>e.start('a'),{code:'locked'});assert.equal(e.status('a').state,'idle');}));
test('uncooperative discovery timeout retains capacity until settlement and never accumulates requests',()=>fixture(async f=>{const gate=defer();let calls=0;const e=f.make({operationTimeoutMs:10,transport:()=>({...f.transport,async listLabels(){calls++;await gate.promise;return f.transport.listLabels();}})});e.start('a');await until(()=>e.status('a').state==='attention');assert.throws(()=>e.start('a'),{code:'busy'});await delay(30);assert.equal(calls,1);gate.resolve();await delay(20);e.start('a');await until(()=>e.status('a').state==='complete');}));
test('authentication rejection retains a retry job and resumes after credentials are renewed',()=>fixture(async f=>{let reject=true;const e=f.make({transport:()=>({...f.transport,async consumeRaw(id,sink){if(reject)throw new GmailTransportError('access_token_rejected');return f.transport.consumeRaw(id,sink);}})});e.start('a');await until(()=>e.status('a').state==='attention');assert.equal(e.status('a').failed,0);assert.equal(f.db.get("SELECT count(*) AS n FROM mail_sync_jobs WHERE state='retry'").n,1);reject=false;f.db.run("UPDATE mail_sync_jobs SET available_at=0 WHERE state='retry'");e.start('a');await until(()=>e.status('a').state==='complete');}));
test('pause invalidates discovery even when durable pause update fails',()=>fixture(async f=>{const gate=defer(),entered=defer();const e=f.make({transport:()=>({...f.transport,async listMessages(i){entered.resolve();await gate.promise;return f.transport.listMessages(i);}})});e.start('a');await entered.promise;f.db.exec("CREATE TRIGGER reject_pause BEFORE UPDATE ON mail_gmail_runs WHEN NEW.state='paused' BEGIN SELECT RAISE(ABORT,'synthetic'); END");assert.throws(()=>e.pause('a'));gate.resolve();await delay(30);assert.equal(e.status('a').enumerated,0);assert.equal(e.status('a').state,'paused');}));
test('failed raw sink releases awaited producer before any retry',()=>fixture(async f=>{let settled=0;const e=f.make({transport:()=>({...f.transport,async consumeRaw(id,sink){try{await sink(Buffer.alloc(65536));return{id,threadId:'thread',labelIds:[],historyId:'1',internalDate:'1000',sizeEstimate:65536,rawBytes:65536};}finally{settled++;}}})});f.db.exec("CREATE TRIGGER fail_blob_chunk BEFORE INSERT ON mail_blob_chunks BEGIN SELECT RAISE(ABORT,'synthetic disk failure'); END");e.start('a');await until(()=>e.status('a').state==='waiting');assert.equal(settled,1);assert.equal(e.status('a').downloaded,0);}));

test('persisted complete status rechecks current raw and projection availability',()=>fixture(async f=>{const e=f.make();e.start('a');await until(()=>e.status('a').state==='complete');f.db.run("DELETE FROM mail_content_manifests WHERE account_id='a' AND kind='body'");assert.equal(e.status('a').state,'attention');assert.equal(e.status('a').error,'content_incomplete');assert.equal(e.status('a').projected,0);}));

test('raw-handler acquisition timeout fences the next queued job and retains original physical operation',()=>fixture(async f=>{let calls=0;const gate=defer();const e=f.make({operationTimeoutMs:15,access:{async acquire(){calls++;if(calls===4)await gate.promise;return f.creds().readAccess('a',binding);}},transport:()=>({...f.transport,async listMessages(){return{messages:[{id:'one',threadId:'t'},{id:'two',threadId:'t'}],nextPageToken:null};}})});e.start('a');await until(()=>e.status('a').state==='attention');assert.equal(calls,4);assert.equal(e.status('a').enumerated,2);assert.equal(e.status('a').downloaded,0);assert.equal(e.status('a').failed,0);assert.throws(()=>e.start('a'),{code:'busy'});await delay(40);assert.equal(calls,4);gate.resolve();await delay(25);assert.equal(e.status('a').downloaded,0);f.db.run("UPDATE mail_sync_jobs SET available_at=0 WHERE state='retry'");e.start('a');await until(()=>e.status('a').state==='complete');}));
test('history pages durably add mail, apply unread deltas and retain remotely deleted originals',()=>fixture(async f=>{
 const requests=[];const e=f.make({transport:()=>({...f.transport,async listHistory(i){requests.push(i);return i.pageToken?{historyId:'30',nextPageToken:null,records:[{id:'25',changes:[{kind:'labelsRemoved',messageId:'recent',threadId:'thread',labelIds:['UNREAD']},{kind:'deleted',messageId:'middle',threadId:'thread',labelIds:[]}]}]}:{historyId:'30',nextPageToken:'page2',records:[{id:'20',changes:[{kind:'added',messageId:'new',threadId:'thread',labelIds:[]},{kind:'labelsAdded',messageId:'recent',threadId:'thread',labelIds:['UNREAD']}]}]};}})});
 e.start('a');await until(()=>e.status('a').state==='complete');assert.equal(requests.length,2);assert.equal(requests[0].startHistoryId,'1');assert.equal(requests[1].startHistoryId,'1');assert.equal(requests[1].pageToken,'page2');assert.equal(f.db.get('SELECT history_id FROM mail_gmail_runs').history_id,'30');assert.equal(e.status('a').removed,1);assert.equal(e.status('a').retained,1);assert.equal(e.status('a').downloaded,3);assert.equal(f.db.get("SELECT count(*) AS n FROM mail_memberships WHERE folder_id='UNREAD'").n,0);assert.equal(f.db.get("SELECT count(*) AS n FROM mail_content_manifests WHERE kind='raw'").n,4);assert.equal(f.db.get('SELECT internal_date FROM mail_gmail_metadata WHERE account_id=? AND message_key=?',['a',JSON.stringify(['gmail','new'])]).internal_date,1000);assert.equal(f.db.get('SELECT count(*) AS n FROM mail_memberships WHERE account_id=? AND message_key=? AND folder_id=?',['a',JSON.stringify(['gmail','new']),'INBOX']).n,1);
}));
test('pause/reopen between history pages retains original anchor and continuation',()=>fixture(async f=>{
 const gate=defer(),entered=defer();let resumed=false;const requests=[];
 const factory=()=>({...f.transport,async listHistory(i){requests.push(i);if(i.pageToken&&!resumed){entered.resolve();await gate.promise;}return i.pageToken?{historyId:'9',nextPageToken:null,records:[]}:{historyId:'9',nextPageToken:'next',records:[{id:'8',changes:[{kind:'labelsAdded',messageId:'recent',threadId:'thread',labelIds:['UNREAD']}]}]};}});
 const e=f.make({transport:factory});e.start('a');await entered.promise;assert.equal(f.db.get('SELECT history_id FROM mail_gmail_runs').history_id,'1');e.pause('a');gate.resolve();await delay(25);assert.equal(f.db.get('SELECT history_page_token FROM mail_gmail_runs').history_page_token,'next');await f.reopen();resumed=true;const next=f.make({transport:factory});next.start('a');await until(()=>next.status('a').state==='complete');assert.equal(requests.at(-1).pageToken,'next');assert.equal(requests.at(-1).startHistoryId,'1');assert.equal(f.db.get('SELECT history_id FROM mail_gmail_runs').history_id,'9');
}));
test('expired history reconciles fresh generation, uses minimal existing snapshots and retains absent originals',()=>fixture(async f=>{
 let expired=false,raw=0,minimal=0,anchors=0;const generations=[];
 const e=f.make({transport:()=>({...f.transport,async getProfile(){anchors++;return{historyId:expired?'100':'1'};},async listMessages(i){if(!expired)return f.transport.listMessages(i);return{messages:i.recentAfterSeconds?[{id:'recent',threadId:'thread'}]:[{id:'recent',threadId:'thread'},{id:'old',threadId:'thread'}],nextPageToken:null};},async listHistory(i){generations.push(f.db.get('SELECT generation FROM mail_gmail_runs').generation);if(!expired){expired=true;throw new GmailTransportError('not_found');}return{historyId:'100',nextPageToken:null,records:[]};},async getMetadata(id){minimal++;return{...await f.transport.getMetadata(id),historyId:'100'};},async consumeRaw(id,sink){raw++;return f.transport.consumeRaw(id,sink);}})});
 e.start('a');await until(()=>e.status('a').state==='complete');assert.equal(anchors,2);assert.notEqual(generations[0],generations[1]);assert.equal(raw,3);assert.equal(minimal,2);assert.equal(e.status('a').removed,1);assert.equal(e.status('a').retained,1);assert.equal(f.db.get("SELECT count(*) AS n FROM mail_content_manifests WHERE kind='raw'").n,3);
}));
test('history checkpoint transaction failure rolls back label changes and page cursor together',()=>fixture(async f=>{
 const e=f.make({transport:()=>({...f.transport,async listHistory(){return{historyId:'20',nextPageToken:null,records:[{id:'20',changes:[{kind:'labelsAdded',messageId:'recent',threadId:'thread',labelIds:['UNREAD']}]}]};}})});
 f.db.exec("CREATE TRIGGER fail_history_checkpoint BEFORE UPDATE ON mail_gmail_runs WHEN NEW.history_id='20' BEGIN SELECT RAISE(ABORT,'synthetic'); END");e.start('a');await until(()=>e.status('a').state==='waiting');assert.equal(f.db.get('SELECT history_id FROM mail_gmail_runs').history_id,'1');assert.equal(f.db.get("SELECT count(*) AS n FROM mail_memberships WHERE folder_id='UNREAD'").n,0);assert.equal(f.db.get("SELECT count(*) AS n FROM mail_sync_scopes WHERE scope_id='gmail:history'").n,0);
}));
test('one incomplete original remains visible while later history receives new mail and removal resolves it',()=>fixture(async f=>{
 let round=0;const e=f.make({pollIntervalMs:1000,transport:()=>({...f.transport,async listMessages(){return{messages:[{id:'bad',threadId:'t'}],nextPageToken:null};},async consumeRaw(id,sink){if(id==='bad')throw new GmailTransportError('raw_too_large');return f.transport.consumeRaw(id,sink);},async listHistory(i){round++;return round===1?{historyId:'10',nextPageToken:null,records:[]}:{historyId:'20',nextPageToken:null,records:[{id:'20',changes:[{kind:'added',messageId:'later',threadId:'t',labelIds:[]},{kind:'deleted',messageId:'bad',threadId:'t',labelIds:[]}]}]};}})});
 e.start('a');await until(()=>e.status('a').state==='attention'&&round===1);assert.equal(e.status('a').failed,1);await until(()=>e.status('a').state==='complete');assert.ok(round>=2);assert.equal(e.status('a').downloaded,1);assert.equal(e.status('a').removed,1);assert.equal(e.status('a').failed,0);
}));
test('raw404 records authoritative provider absence instead of a permanently incomplete known message',()=>fixture(async f=>{
 let profiles=0;const e=f.make({transport:()=>({...f.transport,async getProfile(){profiles++;return{historyId:profiles===1?'1':'9'};},async listMessages(){return{messages:[{id:'gone',threadId:'t'}],nextPageToken:null};},async consumeRaw(){throw new GmailTransportError('not_found');},async listHistory(){return{historyId:'9',nextPageToken:null,records:[]};}})});e.start('a');await until(()=>e.status('a').state==='complete');assert.equal(e.status('a').removed,1);assert.equal(e.status('a').retained,0);assert.equal(e.status('a').failed,0);assert.equal(profiles,1);
}));
test('legacy completed backfill without pre-enumeration anchor reconciles a new generation before catch-up',()=>fixture(async f=>{
 const old=f.make();old.start('a');await until(()=>old.status('a').state==='complete');await old.close();const generation=f.db.get('SELECT generation FROM mail_gmail_runs').generation;
 f.db.run("UPDATE mail_gmail_runs SET state='complete',phase='backfill',history_id=NULL,history_page_token=NULL,poll_at=NULL");let anchors=0,minimal=0;
 const next=f.make({transport:()=>({...f.transport,async getProfile(){anchors++;return{historyId:'10'};},async getMetadata(id){minimal++;return{...await f.transport.getMetadata(id),historyId:'10'};},async listHistory(i){assert.equal(i.startHistoryId,'10');return{historyId:'10',nextPageToken:null,records:[]};}})});next.start('a');await until(()=>next.status('a').state==='complete');assert.notEqual(f.db.get('SELECT generation FROM mail_gmail_runs').generation,generation);assert.equal(anchors,1);assert.equal(minimal,3);assert.equal(next.status('a').downloaded,3);
}));
